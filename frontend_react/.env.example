# Example environment configuration for the React frontend

# URL of the backend API, including protocol and (optionally) /api suffix.
# Example: https://your-backend-domain.com/api
REACT_APP_BACKEND_URL=http://localhost:8000/api
